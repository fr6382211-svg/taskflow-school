export function cn(...v:Array<string|false|null|undefined>){return v.filter(Boolean).join(' ')}
export function formatDateTime(value?:string){if(!value)return '—';const d=new Date(value);if(Number.isNaN(d.getTime()))return '—';return new Intl.DateTimeFormat('id-ID',{dateStyle:'medium',timeStyle:'short'}).format(d)}
export function formatDate(value?:string){if(!value)return '—';const d=new Date(`${value}T00:00:00`);return Number.isNaN(d.getTime())?'—':new Intl.DateTimeFormat('id-ID',{dateStyle:'medium'}).format(d)}
export function errorMessage(e:unknown){const m=e instanceof Error?e.message:String(e||'');if(/invalid login credentials/i.test(m))return 'Email atau password tidak benar.';if(/email not confirmed/i.test(m))return 'Email belum dikonfirmasi. Periksa inbox Anda.';if(/jwt|auth/i.test(m)&&/session/i.test(m))return 'Sesi Anda telah berakhir. Silakan login kembali.';if(/permission|row-level security|not authorized/i.test(m))return 'Anda tidak memiliki izin untuk tindakan ini.';if(/network|fetch/i.test(m))return 'Tidak dapat terhubung ke server. Periksa koneksi internet Anda.';if(/file size/i.test(m))return 'Ukuran file terlalu besar.';return m||'Terjadi kesalahan. Silakan coba lagi.'}
export function makeDueAt(date:string,time:string){return new Date(`${date}T${time}:00`).toISOString()}
export function isOverdue(t:{status:string;dueDate:string;dueTime:string}){return t.status!=='completed'&&t.status!=='submitted'&&new Date(`${t.dueDate}T${t.dueTime}:00`).getTime()<Date.now()}

export function relativeDeadline(t:{dueDate:string;dueTime:string}){const ms=new Date(`${t.dueDate}T${t.dueTime}:00`).getTime()-Date.now();if(ms<=0)return 'Terlambat';const days=Math.floor(ms/86400000);if(days>=2)return `Deadline ${days} hari lagi`;if(days===1)return 'Deadline besok';const hours=Math.max(1,Math.floor(ms/3600000));return `Deadline ${hours} jam lagi`}

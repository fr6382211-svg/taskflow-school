import { CalendarDays, ClipboardList, Gauge, MoreHorizontal, Clock3 } from 'lucide-react';
import { NavLink } from 'react-router-dom';
import { useState } from 'react';
import { cn } from '../../lib/utils';
const links=[['/dashboard','Beranda',Gauge],['/tasks','Tugas',ClipboardList],['/schedule','Jadwal',Clock3],['/calendar','Kalender',CalendarDays]] as const;
export default function MobileNav(){const [open,setOpen]=useState(false);return <><div className="fixed bottom-0 left-0 right-0 z-40 border-t border-slate-200 bg-white/95 p-2 backdrop-blur lg:hidden"><div className="mx-auto grid max-w-md grid-cols-5 gap-1">{links.map(([to,l,I])=><NavLink key={to} to={to} className={({isActive})=>cn('flex flex-col items-center gap-1 rounded-xl px-2 py-2 text-[10px] font-semibold',isActive?'bg-blue-50 text-blue-700':'text-slate-500')}><I size={18}/>{l}</NavLink>)}<button onClick={()=>setOpen(!open)} className={cn('flex flex-col items-center gap-1 rounded-xl px-2 py-2 text-[10px] font-semibold',open?'bg-slate-100 text-slate-800':'text-slate-500')}><MoreHorizontal size={18}/>Lainnya</button></div></div>{open&&<div className="fixed bottom-[72px] left-3 right-3 z-40 rounded-2xl border border-slate-200 bg-white p-2 shadow-xl"><div className="grid grid-cols-2 gap-2"><NavLink to="/notifications" className="rounded-xl p-3 text-sm font-semibold hover:bg-slate-50">🔔 Notifikasi</NavLink><NavLink to="/profile" className="rounded-xl p-3 text-sm font-semibold hover:bg-slate-50">👤 Profil</NavLink><NavLink to="/focus" className="rounded-xl p-3 text-sm font-semibold hover:bg-slate-50">🧠 Focus Mode</NavLink><NavLink to="/insights" className="rounded-xl p-3 text-sm font-semibold hover:bg-slate-50">📊 Insight</NavLink><NavLink to="/settings" className="rounded-xl p-3 text-sm font-semibold hover:bg-slate-50">⚙ Pengaturan</NavLink></div></div>}</>}

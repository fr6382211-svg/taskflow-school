import { useState } from 'react';
import { Link } from 'react-router-dom';
import { Mail, ArrowLeft } from 'lucide-react';
import Button from '../components/ui/Button';
import { useAuth } from '../context/AuthContext';
import { errorMessage } from '../lib/utils';
import { AuthLayout } from './Login';
export default function ForgotPassword(){const {resetPassword}=useAuth();const [email,setEmail]=useState('');const [loading,setLoading]=useState(false);const [message,setMessage]=useState('');const [error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setMessage('');setError('');try{await resetPassword(email.trim());setMessage('Jika email terdaftar, instruksi reset password sudah dikirim.');}catch(err){setError(errorMessage(err));}finally{setLoading(false)}}return <AuthLayout eyebrow="Account recovery"><div className="mb-8"><h1 className="text-3xl font-extrabold tracking-tight">Reset password</h1><p className="mt-2 text-sm leading-6 text-slate-500">Masukkan email akunmu untuk menerima instruksi penggantian password.</p></div><form onSubmit={submit} className="space-y-4"><label className="block"><span className="mb-1.5 block text-xs font-bold uppercase tracking-wider text-slate-500">Email</span><div className="relative"><Mail className="absolute left-3 top-1/2 -translate-y-1/2 text-slate-400" size={16}/><input className="input pl-10" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div></label>{message&&<div className="rounded-xl border border-emerald-100 bg-emerald-50 px-3 py-2.5 text-sm font-semibold text-emerald-700">{message}</div>}{error&&<div className="rounded-xl border border-rose-100 bg-rose-50 px-3 py-2.5 text-sm font-semibold text-rose-700">{error}</div>}<Button loading={loading} type="submit" className="w-full">Kirim instruksi</Button><Link to="/login" className="focus-ring flex items-center justify-center gap-2 text-sm font-semibold text-slate-600"><ArrowLeft size={15}/> Kembali ke login</Link></form></AuthLayout>}

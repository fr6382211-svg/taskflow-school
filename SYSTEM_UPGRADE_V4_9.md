# Fathur School Hub v4.9 — System-wide additive upgrade

This release preserves existing routes and features and adds: Academic Command Center, Location Intelligence page, Activity & Security page, and Task PDF Summary shortcut. It also keeps Fathur tutoring schedule display-only and separate from tasks.

Build: `npm install` then `npm run build`.

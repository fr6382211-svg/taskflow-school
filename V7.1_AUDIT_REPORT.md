# Fathur SchoolHub V7.1 — Audit & Definition of Done Report

## Scope
Upgrade terhadap baseline V6/V7 tanpa menghapus file existing.

## Preservation
- Baseline file count: 302
- Final project file count: 320 (source + config/docs; generated build-cache artifacts removed)
- Existing baseline files removed: 0

## Implemented
- Supabase-backed Weekly Goal and Insight Snapshot.
- Persistent collaboration events for Study Buddy / presence.
- Mobile first navigation: Dashboard / Tasks / Calendar / Focus / More.
- Realtime workspace chat with conversations, members, messages, reads, reactions.
- RLS for all new chat and notification-preference tables.
- Offline connection states and IndexedDB pending mutation queue.
- PWA manifest, service worker, installable shell.
- Universal search expanded to subjects, teachers, and user notifications.
- Attendance & Routine user-facing page backed by attendance_logs.
- Calendar integrated view now includes tasks, school schedule, tutoring, holidays, and focus sessions.
- AI Study Advisor provider abstraction with deterministic fallback and optional secure Edge Function gateway.
- Notification idempotency key support and mark-all-read helper.

## Static verification
- TypeScript/TSX transpile syntax diagnostics: 0 across 195 files.
- Frontend secret scan: no service-role/AI secret literal detected.
- Baseline removal check: 0 removed files.

## Build status
`npm run build` could not be completed in this runtime because package installation timed out and the generated `node_modules` tree was incomplete. The project is therefore **not claimed as build-verified** in this environment.

## Required deployment configuration
### Frontend
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_PUBLISHABLE_KEY`
- Optional `VITE_AI_GATEWAY_URL`

### Supabase Edge Function: study-advisor
- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `AI_API_URL`
- `AI_API_KEY`
- `AI_MODEL`
- `ALLOWED_ORIGIN`

### Existing daily digest
Keep its existing server-side secrets such as `SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `APP_URL`, and `CRON_SECRET` in Edge Function/secret storage only.

## External API dependencies
- Google Calendar / Holiday data: requires configured Google access.
- AI Study Advisor generative mode: optional; deterministic engine is the fallback.
- Firebase/TimeBox/Watch Party: continue to use existing Firebase integration.

## Known limitation
Browser background geolocation remains subject to browser/device permission and lifecycle limits; the system does not silently enable sensitive tracking.

# SearchPanel build fix

- Added local `src/components/ui/input.tsx` bridge to the internal SchoolHub Input.
- Typed the SearchPanel input change event as `ChangeEvent<HTMLInputElement>`.
- No feature logic was removed.

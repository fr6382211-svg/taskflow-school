# Fathur School Hub v4.9.1

- Fixed Dashboard `now` initialization order before `useFathurTutoringSchedule(now)`.
- Hardened tutoring panel error rendering to a string-safe ReactNode.
- Corrected tutoring `next` selection to sort by real schedule date and time.
- Existing routes and features are preserved.

import { useState } from 'react';
import { Bell, Search, Menu, X } from 'lucide-react';
import { useLocation, useNavigate } from 'react-router-dom';
import { useAuth } from '../../context/AuthContext';
import Sidebar from './Sidebar';
import MobileNav from './MobileNav';
import { cn } from '../../lib/utils';
import { useNotifications } from '../../hooks/useNotifications';
export default function AppShell({children}:{children:React.ReactNode}){const [collapsed,setCollapsed]=useState(false);const [mobileOpen,setMobileOpen]=useState(false);const {profile}=useAuth();const {unreadCount}=useNotifications();const nav=useNavigate();const loc=useLocation();const title=loc.pathname.startsWith('/admin')?'Admin':loc.pathname==='/dashboard'?'Dashboard':loc.pathname.split('/')[1]?.replace(/-/g,' ')||'Dashboard';return <div className="app-shell flex bg-slate-50"><Sidebar collapsed={collapsed} setCollapsed={setCollapsed}/><div className="min-w-0 flex-1"><header className="sticky top-0 z-30 flex h-16 items-center gap-3 border-b border-slate-200 bg-white/90 px-4 backdrop-blur md:px-6"><button onClick={()=>setMobileOpen(true)} className="focus-ring rounded-xl p-2 text-slate-600 hover:bg-slate-100 lg:hidden"><Menu size={20}/></button><div className="min-w-0 flex-1"><div className="text-xs font-semibold uppercase tracking-wider text-slate-400">Taskflow School</div><div className="truncate text-sm font-bold capitalize text-slate-800">{title}</div></div><button onClick={()=>nav('/search')} className="focus-ring hidden items-center gap-2 rounded-xl border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-500 md:flex"><Search size={15}/> Cari tugas...</button><button onClick={()=>nav('/notifications')} className="focus-ring relative rounded-xl p-2.5 text-slate-600 hover:bg-slate-100" aria-label="Notifikasi"><Bell size={19}/>{unreadCount>0&&<span className="absolute right-1 top-1 grid h-4 min-w-4 place-items-center rounded-full bg-blue-600 px-1 text-[9px] font-bold text-white">{unreadCount>9?'9+':unreadCount}</span>}</button><button onClick={()=>nav('/profile')} className="focus-ring hidden items-center gap-2 md:flex"><div className="grid h-9 w-9 place-items-center overflow-hidden rounded-full bg-blue-100 text-xs font-bold text-blue-700">{profile?.photoURL?<img src={profile.photoURL} alt="" className="h-full w-full object-cover"/>:(profile?.name?.slice(0,2).toUpperCase()||'U')}</div><div className="hidden text-left xl:block"><div className="max-w-28 truncate text-xs font-bold text-slate-800">{profile?.name||'Pengguna'}</div><div className="text-[10px] text-slate-400">{profile?.role==='admin'?'Administrator':'Siswa'}</div></div></button></header><main className="mx-auto w-full max-w-[1600px] p-4 pb-24 md:p-6 lg:pb-6">{children}</main></div><MobileNav/>{mobileOpen&&<div className="fixed inset-0 z-50 bg-slate-950/35 lg:hidden" onMouseDown={e=>e.target===e.currentTarget&&setMobileOpen(false)}><div className="h-full w-[280px] bg-white p-4 shadow-2xl"><div className="mb-4 flex items-center justify-between"><div className="text-lg font-extrabold">TASKFLOW</div><button className="p-2" onClick={()=>setMobileOpen(false)}><X size={20}/></button></div><div className="space-y-1"><button onClick={()=>{setMobileOpen(false);nav('/dashboard')}} className="w-full rounded-xl p-3 text-left text-sm font-semibold hover:bg-slate-50">Dashboard</button><button onClick={()=>{setMobileOpen(false);nav('/tasks')}} className="w-full rounded-xl p-3 text-left text-sm font-semibold hover:bg-slate-50">Tugas</button><button onClick={()=>{setMobileOpen(false);nav('/schedule')}} className="w-full rounded-xl p-3 text-left text-sm font-semibold hover:bg-slate-50">Jadwal</button><button onClick={()=>{setMobileOpen(false);nav('/calendar')}} className="w-full rounded-xl p-3 text-left text-sm font-semibold hover:bg-slate-50">Kalender</button><button onClick={()=>{setMobileOpen(false);nav('/profile')}} className="w-full rounded-xl p-3 text-left text-sm font-semibold hover:bg-slate-50">Profil</button><button onClick={()=>{setMobileOpen(false);nav('/settings')}} className="w-full rounded-xl p-3 text-left text-sm font-semibold hover:bg-slate-50">Pengaturan</button></div></div></div>}</div>}

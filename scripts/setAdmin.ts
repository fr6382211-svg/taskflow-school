import {createClient} from '@supabase/supabase-js';
const url=process.env.SUPABASE_URL;const key=process.env.SUPABASE_SERVICE_ROLE_KEY;const email=process.argv[2];if(!url||!key||!email){console.error('Usage: SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npm run set:admin -- email@example.com');process.exit(1)}const db=createClient(url,key,{auth:{persistSession:false}});const run=async()=>{const {data,error}=await db.auth.admin.listUsers({page:1,perPage:1000});if(error)throw error;const u=data.users.find(x=>x.email?.toLowerCase()===email.toLowerCase());if(!u)throw new Error('User tidak ditemukan. Register dulu melalui aplikasi.');const {error:up}=await db.from('users').update({role:'admin'}).eq('id',u.id);if(up)throw up;console.log(`Role admin set for ${email}.`) };run().catch(e=>{console.error(e);process.exit(1)})

# Fathur SchoolHub V7.2.1 — V23 SQL Fix

This package is V7.2 with the SQL migration 0023 repaired for PostgreSQL type compatibility.

Fixed:
- `storage.objects.owner_id` (text) vs `auth.uid()` (uuid): uses `auth.uid()::text`.
- safer UUID parsing for chat attachment policy paths.
- idempotent policy replacement with `DROP POLICY IF EXISTS`.

No application feature is intentionally removed by this repair.

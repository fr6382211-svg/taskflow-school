# v5.4.1.1 Build Fix

- Adds Vite aliases for `@schoolhub/ui`, `@boredkevin/ui`, `@bkui`, and `@shui`.
- Adds the missing `src/components/ui/input.tsx` bridge.
- Keeps the existing MediaBox/Remote implementation intact.

Run from project root:

```powershell
npm install
npm run build
```

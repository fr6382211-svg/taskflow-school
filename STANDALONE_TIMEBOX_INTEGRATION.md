# Standalone TimeBox Integration

The School Hub menu entries TimeBox, MediaBox, and Watch Party now navigate directly to:
https://fathur-rahman.vercel.app

Legacy in-app routes `/timebox`, `/mediabox`, and `/watch-party` also redirect to the same standalone application.

Admin access is visible/allowed when either the Supabase profile role or auth metadata role is `admin`.
